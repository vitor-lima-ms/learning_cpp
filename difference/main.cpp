#include <iostream>
using namespace std;

int main() {
    int A, B, C, D;
    int diff;

    cin >> A >> B >> C >> D;

    diff = (A * B) - (C * D);

    cout << "DIFERENCA = " << diff << "\n";

    return 0;
    }
